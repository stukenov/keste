# Выбор и объединение ячеек

**Дата:** 5 октября 2025  
**Версия:** v0.6.1  
**Статус:** Реализовано ✅

---

## 📝 Обзор

Добавлен функционал множественного выбора ячеек (range selection) для объединения и работы с диапазонами.

---

## 🎯 Как использовать

### 1. Выбор одной ячейки

**Способ:** Просто кликните на ячейку

```
Клик → Выбрана одна ячейка
```

### 2. Выбор диапазона ячеек (Shift + Click)

**Способ:** Выберите первую ячейку, затем **удерживайте Shift** и кликните на последнюю ячейку

```
1. Клик на A1
2. Shift + Клик на C3
   → Выбран диапазон A1:C3 (9 ячеек)
```

**Визуально:** 
- Первая выбранная ячейка будет **ярко подсвечена** (темно-зелёная граница)
- Остальные ячейки в диапазоне будут **слегка подсвечены** (светло-зелёный фон)

### 3. Выбор диапазона ячеек (Drag / Перетаскивание)

**Способ:** Зажмите левую кнопку мыши на первой ячейке и тянИте до последней

```
Зажать ЛКМ на A1 → Тянуть до C3 → Отпустить
   → Выбран диапазон A1:C3 (9 ячеек)
```

### 4. Объединение выбранных ячеек

**Способ:** После выбора диапазона нажмите кнопку **"Merge Cells"** на панели инструментов

```
1. Выберите диапазон (любым из способов выше)
2. Нажмите "Merge Cells" на панели
   → Ячейки объединены!
```

**Альтернатива без выбора диапазона:**
- Если не выбран диапазон, кнопка "Merge Cells" объединит 2×2 ячейки вокруг текущей выбранной ячейки

---

## 🎨 Визуальная индикация

### Информация о выбранном диапазоне

Когда выбрано больше 1 ячейки, **справа от Formula Bar** появляется информация:

```
┌─────────────────────────────────────────┐
│ 📊 A1    [formula]    │  3R × 3C (9 cells) │
└─────────────────────────────────────────┘
```

**Где:**
- `3R` = 3 строки (Rows)
- `3C` = 3 столбца (Columns)  
- `9 cells` = всего 9 ячеек в диапазоне

### Цветовая индикация

| Состояние | Визуальный стиль |
|-----------|------------------|
| **Активная ячейка** (anchor) | Тёмная зелёная граница + светло-зелёный фон |
| **Ячейки в диапазоне** | Очень светло-зелёный фон |
| **Hover** (наведение) | Светло-серый фон |

---

## ⌨️ Горячие клавиши

| Комбинация | Действие |
|------------|----------|
| **Shift + Click** | Выбрать диапазон от текущей ячейки до клика |
| **Drag (ЛКМ)** | Выбрать диапазон перетаскиванием |
| **Esc** | Снять выделение диапазона |
| **Arrow Keys** | Навигация по ячейкам (сбрасывает выделение диапазона) |

---

## 🔧 Технические детали

### Типы данных

```typescript
// Позиция одной ячейки
interface CellPosition {
  row: number;
  col: number;
  sheetId: string;
}

// Выбранный диапазон
interface Selection {
  start: CellPosition;  // Якорная ячейка (первая выбранная)
  end: CellPosition;    // Последняя ячейка в диапазоне
}
```

### API

```typescript
// В useSpreadsheetEditor hook
const {
  selectedCell,              // Текущая активная ячейка
  selection,                 // Выбранный диапазон (null если одна ячейка)
  setSelectedCellWithSelection, // Выбрать ячейку с возможностью расширения диапазона
  mergeCells,                // Объединить ячейки (автоматически использует selection)
} = useSpreadsheetEditor(workbook);

// Использование
setSelectedCellWithSelection(position, extendSelection);
// extendSelection = false → выбрать одну ячейку
// extendSelection = true  → расширить диапазон до этой ячейки

// Объединение
mergeCells(); // Автоматически использует selection если есть
// ИЛИ
mergeCells(startRow, startCol, endRow, endCol); // Ручное указание диапазона
```

---

## 🐛 Известные ограничения

1. **Множественные диапазоны:** Пока поддерживается только один непрерывный диапазон (как в Excel)
2. **Ctrl+Click:** Выбор нескольких несвязанных ячеек пока не поддерживается
3. **Автоматическое объединение:** При наборе текста в объединённых ячейках текст пока не распределяется

---

## 📋 Примеры использования

### Пример 1: Объединение заголовка таблицы

```
Задача: Создать заголовок "Отчёт за Квартал" на ячейках A1:E1

Шаги:
1. Клик на A1
2. Shift + Клик на E1
3. Появится: "1R × 5C (5 cells)"
4. Нажать "Merge Cells"
5. Ввести текст "Отчёт за Квартал"
```

### Пример 2: Объединение для таблицы

```
Задача: Создать объединённую ячейку 3×3 для большого блока текста

Шаги:
1. Клик на B2
2. Зажать ЛКМ и тянуть до D4
3. Появится: "3R × 3C (9 cells)"
4. Нажать "Merge Cells"
```

---

## 🎓 Советы по использованию

### ✅ Рекомендуется

- Используйте **Shift+Click** для точного выбора больших диапазонов
- Используйте **Drag** для быстрого выбора небольших диапазонов
- Смотрите на индикатор размера диапазона перед объединением

### ❌ Не рекомендуется

- Не пытайтесь выбирать диапазоны во время редактирования ячейки
- Не объединяйте слишком большие диапазоны (может быть долго)

---

## 🚀 Будущие улучшения

- [ ] Выбор несвязанных ячеек (Ctrl+Click)
- [ ] Выбор целых строк/столбцов (клик на заголовок)
- [ ] Выбор всех ячеек (Ctrl+A)
- [ ] Подсветка формул при редактировании
- [ ] Автоматическое расширение выделения при вводе

---

## 📚 Связанные документы

- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура проекта
- [PRD-2.md](product/PRD-2.md) - Требования к продукту
- [PHASE6.md](phases/PHASE6.md) - Документация Phase 6

---

**Автор:** Keste Development Team  
**Последнее обновление:** 5 октября 2025
